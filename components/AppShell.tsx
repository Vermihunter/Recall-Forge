import Link from "next/link";
import type {ReactNode} from "react";
const nav=[["01","Dashboard","/"],["02","Roadmap","/roadmap"],["03","Prompt Studio","/prompts"],["04","Question Bank","/questions"],["05","Review","/review"],["06","Print","/print"],["07","Mistakes","/mistakes"]];
export default function AppShell({children}:{children:ReactNode}){return <div className="shell"><aside className="sidebar"><div className="brand"><div className="brand-mark">R</div><div><div className="brand-name">Recall Forge</div><div className="brand-sub">deliberate practice</div></div></div><nav className="nav">{nav.map(([n,label,href])=><Link href={href} key={href}><span className="nav-num">{n}</span><span>{label}</span></Link>)}</nav><div className="sidebar-footer">One session → one chat.<br/>PostgreSQL stores your roadmap, questions, review state, sessions and mistakes.</div></aside><main className="main"><header className="topbar"><div className="topbar-title">Deliberate practice system</div><div className="topbar-right">Learn → Recall → Critique → Apply</div></header><div className="content">{children}</div></main><nav className="mobile-nav">{nav.slice(0,6).map(([n,label,href])=><Link href={href} key={href}><span>{n}</span><small>{label}</small></Link>)}</nav></div>}
